@@ meta
name = load_pickle()
type = method
pclass = Sketch
processing_name = loadPickle
category = input
subcategory = files

@@ signatures
load_pickle(pickle_path: Union[str, Path], **kwargs: dict[str, Any]) -> Any

@@ variables
kwargs: dict[str, Any] - keyword arguments
pickle_path: Union[str, Path] - url or file path for pickle data file

@@ description
The documentation for this field or method has not yet been written. If you know what it does, please help out with a pull request to the relevant file in https://github.com/py5coding/py5generator/tree/master/py5_docs/Reference/api_en/.

@@ example
def setup():
    global mouse_x_positions, mouse_y_positions
    py5.size(250, 250)
    py5.stroke_weight(10)
    data = py5.load_pickle('mouse_positions.pkl')
    mouse_x_positions = data['mouse_x']
    mouse_y_positions = data['mouse_y']


def draw():
    i = py5.frame_count
    if i < len(mouse_x_positions) and i < len(mouse_y_positions):
        py5.point(mouse_x_positions[i], mouse_y_positions[i])
